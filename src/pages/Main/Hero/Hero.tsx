import Button from '@shared/Button';
import { Container, Section } from '@styles/globals';
import React from 'react';

const Hero = () => {
	return (
		<Section>
			<Container>
				<h1>Блок Hero</h1>
				<Button />
			</Container>
		</Section>
	);
};

export default Hero;
